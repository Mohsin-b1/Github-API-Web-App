<?php require("Views/template/header.phtml") ?>
<!-- Page content-->
<div class="container-fluid">
    <h1 class="mt-4 text-center">Pulls</h1>
</div>

<div class="album py-5 bg-light">
    <div class="container">
            <div class="col-md-12">
                <?php
                $repository = $data;
                foreach($pullsData as $value):
                    $user = $value['user'];
                    //var_dump($value['commit']);
                    //echo "<br><br>";
                    //var_dump($commit['author']);
                    //var_dump($author['name']);
                    require('pullsCard.phtml');
                endforeach;
                ?>

                <div class="card mb-12 box-shadow">
                    <div class="card-body text-center">
                        <div class="d-flex justify-content-center align-items-center">
                            <form method="post" action="" class="form">
                                <div class="btn">
                                    <input type="hidden" name="name" value="<?=$repository["full_name"] ?>">
                                    <input type="submit" name="show" value="Back to Repository Info" class="btn btn-outline-secondary" >
                                </div>
                            </form>

                        </div>
                    </div>
                </div>

            </div>
    </div>
</div>

<?php require ('template/footer.phtml') ?>

